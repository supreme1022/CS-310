Person is an abstract base class for Student and Instructor, providing a common interface for serialization. JSON and YAML use nlohmann::json and yaml-cpp, while CSV ensures fields with commas are properly quoted. Constructors throw ValidationError for invalid IDs, empty names/emails, or malformed data. This design keeps serialization consistent across derived classes and enforces input validation.